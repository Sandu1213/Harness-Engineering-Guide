# Diagrams

`mermaid/` 保存 Mermaid 源码，是图示的事实来源；`exported/` 保存从源码导出的 SVG、PNG 或其他发布资源。空目录中的 `.gitkeep` 仅用于保留目录，真正图示应使用 `chapter-NN-topic.mmd` 命名。

每张图必须：

1. 在正文中回答一个明确问题，并附读图说明。
2. 使用与 `.ai/glossary.md` 一致的节点名称。
3. 在导出前审查边界、箭头含义、许可证和可访问的替代描述。

不要把唯一的编辑版本放在二进制图片中。
