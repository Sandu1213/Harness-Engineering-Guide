# Example Prompt

为指定章节创建最小、可验证的示例。先写清楚：读者问题、环境前提、输入、输出、验证命令、成功条件、失败边界和安全限制。

优先标准库与项目已有依赖；不要添加不必要框架。示例应从最小闭环开始，再列出可选增强。没有实际运行时，使用“预期输出”而不是“实际结果”，并明确说明。

将代码和 README 放在 `examples/` 对应目录，必要时补充测试；同步更新章节和进度表。
