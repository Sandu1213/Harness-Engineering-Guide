# Style Guide

## 语言、语气与读者

使用简体中文，面向有编程基础的工程读者。语气平实、具体、可验证：先描述工程问题，再说明机制、权衡与操作。避免“革命性”“显著提升”等无证据形容词，以及“让我们深入探索”式空泛引导。

## 中英文、术语与缩略词

- 中文与英文、数字之间保留一个半角空格：`使用 Context Engineering 管理 8 个来源`。
- 首次出现写作“上下文工程（Context Engineering）”；后续可只用中文或约定英文。
- 缩略词首次给全称：`模型上下文协议（Model Context Protocol，MCP）`。
- 术语以 [.ai/glossary.md](.ai/glossary.md) 为准；不要为同一概念创造近义名。

## 标题与段落

- 每篇正式章节只有一个一级标题；`##` 表示主要模块，`###` 表示其子问题，避免跳级。
- 标题使用能回答问题的名词短语或动宾结构，不使用“杂谈”“其他”等泛标题。
- 一个段落表达一个完整推理步骤；通常 2 至 5 句，过长时拆成带因果关系的小段。
- 总结必须回收本章目标、条件和边界，禁止只重复“我们学到了很多”。

## 列表、表格与提示框

- 有顺序的步骤用编号列表；并列条件用无序列表；列表项保持句式平行。
- 表格用于稳定字段比较，不用于承载多段论证；窄屏可读性优先。
- 提示框使用明确前缀而非视觉花样：`> 注意：`、`> 风险：`、`> TODO(verify)：`。

## 代码与命令

- 代码块必须标注语言，如 `bash`、`python`、`typescript`、`mermaid`。
- 命令只展示已存在或在上下文中说明的路径、变量与工具；真实执行结果与预期示例严格分开。
- 省略关键实现时明确说明“为聚焦 X，省略 Y”，并链接完整示例。

## Mermaid 与图示

- Mermaid 代码块使用 `mermaid` 标记，源文件置于 `diagrams/mermaid/`。
- 每张图在前后给出问题、读法和结论；图中节点名称应与正文术语一致。
- 图示表达接口、状态或因果关系，不承担装饰职责；复杂图拆分而非缩小字体。

## 引用与交叉引用

- 对可归因事实使用行内链接或脚注式来源；详细条目同步登记到 `.ai/references.md`。
- 区分“来源指出”“本书推导”“示例假设”。直接引语必须短且必要。
- 使用相对链接连接仓库内容；重命名后运行 `npm run check:links`。

## 文件与章节命名

- 章节文件采用 `NN-english-kebab-case.md`，例如 `01-prompt-to-harness.md`。
- 正式章节标题使用中文；文件名使用稳定英文 slug，避免因措辞调整破坏链接。
- 图示文件使用 `chapter-NN-topic.mmd`；示例目录使用对应章节或主题名称。

## 案例写法与技术准确性

每个案例至少回答：背景约束是什么、成功如何观测、关键设计为何如此、失败会怎样、如何验证。不要把伪代码、猜测的 API 或未运行结果伪装为生产事实。动态产品能力在写作当日以官方资料复核；未核验的信息必须写为 `TODO(verify)：说明`。

## 避免 AI 腔

- 用具体主语和动词替代抽象套话，例如“校验脚本拒绝失效链接”，而不是“确保高质量”。
- 用可检查的条件替代程度副词，例如“在 30 秒内完成”而不是“快速完成”。
- 不用模板化的“三点万能总结”；保留真实权衡、反例和不确定性。
