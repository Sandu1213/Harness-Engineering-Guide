# 第 21 章 Diagram Review

## 实际渲染

`chapter-21-project-harness-portability.mmd` 已使用 Mermaid CLI 11.16.0 分别导出 SVG 和 PNG，两个导出命令均成功完成。PNG 已视觉检查：共享工件、两类适配声明、单一任务、验证、证据回写和独立的权限/批准边界均可辨认。

## 一致性与边界

正文 Mermaid 块与 `diagrams/mermaid/chapter-21-project-harness-portability.mmd` 使用同一节点和箭头。图没有表示真实产品加载路径、账户、Sandbox、网络或 Tool 调用；“准入条件”和“证据不等于授权”两条虚线专门避免把上下文或验证混同为权限。
