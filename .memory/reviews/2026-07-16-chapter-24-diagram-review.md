# 第 24 章 Diagram Review

- Mermaid 源描述候选、Profile、Admission、Envelope、Observation 与独立验证的工件流。
- 已确认正文 Mermaid 块与 `.mmd` 源保持一致；SVG/PNG 导出用于发布预览。
- 图中不表示真实 Client、Server、传输、OAuth、权限、调用或外部效果；“可记录为已验证结果”仅在独立验证通过后出现。
