# 第 31 章语言审阅

## 审阅范围

- 正文、示例计划、事实核验、图示替代描述、术语表与相邻章节衔接。
- 术语首现、具体主语、阶段时态、表格句式和来源／模型／运行证据的分层表达。

## 修订

- 将首次出现的 pytest fixture、Playwright Browser Context、locator、Failure Record 与 Report Gate 改为“中文（English）”形式，与 `.ai/glossary.md` 一致。
- 将正文首次出现的 `timeout` 改为“超时（timeout）”，随后保留代码和诊断标签中的精确英文值。
- 保留 API Contract Check、UI Flow Evidence、Test Evidence Plan、`planned`、`ready`、`stopped` 与 `requires_approval` 的接口/状态含义，不改写为真实测试或业务结果。

## 结论

语言审阅只收束中文术语、主语和时态，未改变 CH31-REF-01 至 CH31-REF-05 的限定范围、`assessTestEvidencePlan` 接口、8 项 Node 测试结果、Mermaid 源码或导出图语义。pytest、Playwright、API、浏览器、账户、网络、CI、报告与凭证仍明确为未运行范围。
