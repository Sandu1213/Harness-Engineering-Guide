# 第 7 章 Language Editing

日期：2026-07-15

## 范围

本次只编辑 `docs/part-02-components/07-working-memory-and-long-term-memory.md` 的中文表达、术语首次出现与段落切分。不修改来源范围、事实核验结论、示例接口、测试、Mermaid 源、SVG/PNG 或相邻章节责任。

## 编辑内容

- 将首次出现的 `scope` 改为“作用范围（scope）”，后续叙述在中文语句中优先使用“作用范围”。
- 明确“长期记忆”是可跨任务维护的能力名，而“长期候选（Long-term Candidate）”只表示通过分类、尚未进入实际存储与治理的记录。
- 说明概念表的 `snake_case` 与 JavaScript 示例的 `camelCase` 是命名语境差异，不是两套 Memory Record 模型。
- 拆分读取门槛和工作流程中的长句，保留任务锚点、当前证据、预算与投影边界。

## 核对结论

编辑后，正文、示例的 `observedAt` / `revisionOrRevocation` 字段、生命周期图中的 `Long-term Candidate` 节点以及章节总结的术语一致。未新增产品事实、来源归因、安全承诺或运行结果；第 7 章的纯内存示例与图示仍只表示本书工程模型。

下一阶段为 Final Review。
