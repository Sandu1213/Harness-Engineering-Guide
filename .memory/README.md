# Project Memory

`.memory/` 保存比当前任务更长期的可追溯知识。它不是临时草稿区：决策记录“为何如此”，审查记录“发现了什么”，经验记录“下次怎样更好”。

- `decisions/`：独立的架构与内容决策。
- `reviews/`：章节或系统级审查产物。
- `lessons/`：从失败、返工或验证中提炼的可复用经验。

当前任务状态仍以 `.context/` 和 `.ai/progress.md` 为准。
