---
title: "NN. 章节中文标题"
slug: "NN-english-kebab-case"
status: "research | outline | draft | review | complete"
part: "part-XX"
authors: []
reviewers: []
prerequisites: []
related_chapters: []
diagrams: []
examples: []
references: []
updated_at: "YYYY-MM-DD"
---

# NN. 章节中文标题

> 填写说明：用一句话说明本章解决的工程问题，不复述目录标题。示例：本章把“模型会调用工具”拆解为可验证的工具协议与错误边界。

## 本章目标

> 填写说明：列出 3 至 5 个完成后可观察的能力，使用动词。示例：读者能够为一个文件操作工具定义输入、输出、错误和验证步骤。

- [ ] 目标一：
- [ ] 目标二：
- [ ] 目标三：

## 为什么要学

> 填写说明：从真实失败、成本或责任问题切入。说明不学习会发生什么，以及本章不解决什么。示例：只给工具一句自然语言说明会让失败原因混在模型输出中，难以重试或审计。

## 前置知识

> 填写说明：列出必需的概念和本书前置章节；对陌生读者给出最小补充链接。示例：建议先阅读第 05 章 Instructions 与 Prompt、第 10 章 Workflow 与状态管理。

- 前置章节：
- 技术前提：
- 不要求：

## 场景引入

> 填写说明：描述一个具备约束、输入、风险和成功标准的具体场景。不要以“假设你想构建一个很强的 Agent”开头。

**场景：** 例如，Agent 需要修改一份配置文件，但只能在验证通过后写入，并且必须留下可审查记录。

**成功标准：** 例如，修改前可预览，修改后重新读取文件并通过格式校验。

**边界：** 例如，本章不讨论远程生产环境发布。

## 核心概念

> 填写说明：每个概念遵循“定义 → 机制 → 何时使用 → 不适用边界 → 与其他概念的关系”。首次出现使用“中文（English）”。来源事实需有引用；本书推导明确写为“本书建议”。

### 概念一：名称

定义与边界。`TODO(verify)：` 尚未核验的事实写在这里，不得改写为确定陈述。

### 概念二：名称

解释它如何改变场景中的决策或接口。

## 架构图

> 填写说明：先用一句话说明图回答什么问题，再放 Mermaid。源码另存为 `diagrams/mermaid/chapter-NN-topic.mmd`。图后解释关键箭头，不用图代替论证。

```mermaid
flowchart LR
    Task[任务输入] --> Harness[Harness]
    Harness --> Tool[受控工具]
    Tool --> Evidence[可验证证据]
    Evidence --> Harness
```

图示说明：Harness 根据验证证据决定下一步，而不是把工具返回文本直接视为成功。

## 工作流程

> 填写说明：给出有顺序、状态和失败分支的步骤。每步说明输入、动作、输出和验证点。

1. **读取：** 读取任务与权限边界；输出是可执行计划。
2. **预览：** 生成变更预览；失败时停止并记录原因。
3. **执行：** 在授权范围调用工具；保存工具结果。
4. **验证：** 重新观察目标状态；未满足成功标准则进入恢复或升级。
5. **记录：** 写入状态、证据和下一步。

## 最小示例

> 填写说明：提供最小闭环，说明路径、环境、输入、命令、预期和限制。若未运行，请写“预期输出”，不得写“运行结果”。完整代码应链接到 `examples/`。

```python
def verify_change(before: str, after: str) -> bool:
    """示例：只在内容实际变化时允许后续验证。"""
    return before != after
```

**验证命令：** `TODO(verify)：填写实际命令`

**预期：** 输入不同内容时返回 `True`；真实文件写入还需要独立的格式或业务校验。

## 逐步增强

> 填写说明：说明如何从最小示例增加一个真实约束，每一步只增加一种复杂度，并列出触发升级的条件。

1. 增加结构化错误结果，而不是只返回布尔值。
2. 增加 dry-run 和显式授权，适用于存在写入副作用时。
3. 增加状态持久化，适用于会中断或需要交接的长任务。

## 完整工程案例

> 填写说明：选择与读者工作相关的端到端案例。描述背景、约束、架构、关键决策、运行路径、失败处理和结果证据；避免把完整项目日志贴进书中。

**背景：**

**约束：**

**设计选择：**

**结果与证据：**

## 实现说明

> 填写说明：解释关键接口、数据流、状态迁移和设计取舍。代码逐行讲解只在读者必须理解细节时使用。

| 决策 | 选择 | 原因 | 替代方案与边界 |
| --- | --- | --- | --- |
| 例如：写入方式 | 先预览后执行 | 保留人工确认点 | 纯自动写入只适用于低风险、可回滚任务 |

## 测试与验证

> 填写说明：区分单元、集成、端到端和人工审查。只记录真实执行过的命令与结果；未执行项说明原因和风险。

| 层级 | 验证对象 | 命令或方法 | 成功标准 | 实际状态 |
| --- | --- | --- | --- | --- |
| 单元 | 纯逻辑 | `TODO(verify)` | 输出满足契约 | 未执行 |
| 端到端 | 用户可见流程 | `TODO(verify)` | 操作后重新观察到目标状态 | 未执行 |

## 工程实践

> 填写说明：归纳可以迁移到其他项目的做法，如状态写入、超时、日志、权限、成本或并发控制。每条说明适用条件。

- 实践一：
- 实践二：

## 最佳实践

> 填写说明：给出可操作的推荐，并附上原因；不要重复“保持代码整洁”之类无法验证的建议。

- 推荐：
- 原因：

## 常见错误

> 填写说明：每项包含错误表现、根因、检测方式和修复方向。

| 错误 | 表现 | 根因 | 修复方向 |
| --- | --- | --- | --- |
| 例如：只看工具返回文本 | 实际状态未改变却报告成功 | 未做回读验证 | 执行后重新观察状态 |

## 安全与边界

> 填写说明：明确不可信输入、权限、密钥、数据、不可逆操作和人类审批要求。安全限制不是可选附录。

- 权限边界：
- 数据边界：
- 人工审批点：
- 不适用范围：

## 章节总结

> 填写说明：回收本章目标、关键机制和边界，并连接下一章。不要只重复标题列表。

## 练习

> 填写说明：设计能检查读者是否理解边界和验证方法的练习；答案或提示不得依赖未给出的工具。

1. 为一个只读检索工具补充错误分类与验证步骤。
2. 说明何时不应让 Agent 自动执行该工具。

## 延伸阅读

> 填写说明：只列出与本章问题直接相关的阅读，不用长链接清单替代论证。动态资料标注访问日期。

- `TODO(verify)：来源、用途和访问日期`

## 参考资料

> 填写说明：列出本章实际使用的来源，并同步 `.ai/references.md`。每条标注支持的陈述，不伪造引文。

- `TODO(verify)：REF-NNN — 支持的具体陈述`

## 章节完成检查表

- [ ] Front matter、目标、前置知识和章节依赖完整。
- [ ] 内容为原创表达，来源观点与本书扩展已区分。
- [ ] 每项可归因事实有来源，未核验项明确标记。
- [ ] 图示有 Mermaid 源码、读图说明和一致术语。
- [ ] 示例有环境、验证方式、结果状态和安全边界。
- [ ] 技术、事实、语言和图示审查均已记录。
- [ ] 已运行 `npm run validate` 或记录阻塞。
- [ ] `.ai/progress.md`、`CURRENT_STATE.md`、`NEXT_TASK.md` 与交接已更新。
